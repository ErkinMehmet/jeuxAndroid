<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.SaveTheBunny" parent="Theme.AppCompat.Light.NoActionBar"> <!-- Use AppCompat parent -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>

        <!-- Custom Blue Colors -->
        <item name="android:colorPrimary">@color/blue_primary</item>
        <item name="android:colorPrimaryDark">@color/blue_primary_dark</item>
        <item name="android:colorAccent">@color/blue_accent</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:windowBackground">@color/blue_lightest</item>
        <item name="android:textColor">@color/blue_darkest</item>
    </style>

</resources>
